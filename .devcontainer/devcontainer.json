{
	// Minimaler Dev-Container (Modell B): Codespaces übernimmt Git-Identität + gh-Auth aus dem
	// Account des Entwicklers (eigener Alias). Öffentliches Repo → kein claude-code / Anthropic-Key
	// im Container. Signing im Codespace unsigniert (Squash-Merge wird web-flow-signiert).
	// Details: org-Repo → onboarding/client-setup.md.
	"name": "Joomla dev",
	"image": "mcr.microsoft.com/devcontainers/universal:4.0.1-noble"
}
