{
	// Minimaler Dev-Container (öffentliches Repo → kein claude-code / Anthropic-Key).
	// Commit-Identität, Signing und Tooling regeln die Repo-Konventionen (AGENTS.md, Repo-Root)
	// bzw. die org-Konventionen — der Container setzt davon bewusst nichts automatisch (keine
	// Identität, kein Key). Hinweis: Für dieses öffentliche Repo ist verifiziertes Signing Pflicht.
	"name": "Joomla dev",
	"image": "mcr.microsoft.com/devcontainers/universal:4.0.1-noble"
}
