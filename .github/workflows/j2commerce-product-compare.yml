name: Build & Test - J2Commerce Product Compare

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'j2commerce/plg_j2commerce_productcompare/**'
      - 'shared/**'
      - '.github/workflows/j2commerce-product-compare.yml'
  pull_request:
    branches: [main]
    paths:
      - 'j2commerce/plg_j2commerce_productcompare/**'
      - 'shared/**'
      - '.github/workflows/j2commerce-product-compare.yml'

permissions: {}

env:
  # J2Commerce 6 publishes no release tags. The source is pinned to the commit
  # used in production (default branch as of 2026-08-28) so every run tests the
  # same J2Commerce 6 code instead of the moving default branch.
  J2C6_REF: '7edb6e11ae9148bf996b06c47a0d8266865af7b2'

jobs:
  build:
    name: Build Package
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - name: Build
        run: |
          cd j2commerce/plg_j2commerce_productcompare
          chmod +x build.sh
          ./build.sh
          cp *.zip tests/extension.zip
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: extension-package
          path: j2commerce/plg_j2commerce_productcompare/tests/extension.zip
          retention-days: 1

  syntax-check:
    name: PHP Syntax Check
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - name: Setup PHP
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # v2
        with:
          php-version: '8.2'
      - name: Check PHP syntax
        run: |
          # xargs exits non-zero as soon as one file fails; `find -exec \;` would not.
          find j2commerce/plg_j2commerce_productcompare -name '*.php' -not -path '*/vendor/*' -print0 | xargs -0 -n1 php -l

  language-lint:
    name: Language Files
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - name: Setup PHP
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # v2
        with:
          php-version: '8.3'
      - name: Lint language files
        run: php shared/tests/lang-lint.php j2commerce/plg_j2commerce_productcompare
      - name: Check compatibility requirements (Joomla 5.4+, PHP 8.1+)
        run: php shared/tests/requirements-check.php j2commerce/plg_j2commerce_productcompare
      - name: Scan for deprecated Joomla APIs
        run: php shared/tests/deprecated-api-scan.php j2commerce/plg_j2commerce_productcompare

  test-j5:
    name: ${{ matrix.name }} (J5)
    runs-on: ubuntu-latest
    needs: build
    permissions:
      contents: read
    strategy:
      fail-fast: false
      max-parallel: 1
      matrix:
        include:
          - name: '01 - Installation'
            script: 'installation'
          - name: '02 - Configuration'
            script: 'configuration'
          - name: '03 - Media Files'
            script: 'media'
          - name: '04 - Plugin Class'
            script: 'plugin-class'
          - name: '05 - AJAX Endpoint'
            script: 'ajax'
          - name: '06 - getProductsData'
            script: 'getproductsdata'
          - name: '07 - Asset Injection'
            script: 'asset-injection'
          - name: '08 - Render Injection'
            script: 'render-injection'
          - name: '09 - Event Dispatch'
            script: 'event-dispatch'
          - name: 'Installer Messages'
            script: 'install-messages'
          - name: '10 - Uninstall'
            script: 'uninstall'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: extension-package
          path: j2commerce/plg_j2commerce_productcompare/tests/
      - name: Start environment (J5)
        run: |
          cd j2commerce/plg_j2commerce_productcompare/tests
          docker compose up -d
          echo "Waiting for container to be ready..."
          timeout 300 bash -c 'until docker exec plg_j2commerce_productcompare_test test -f /var/www/html/health.txt 2>/dev/null; do sleep 5; done'
          echo "Container ready"
      - name: Show container logs
        if: always()
        run: |
          cd j2commerce/plg_j2commerce_productcompare/tests
          docker compose logs joomla 2>&1 | tail -100 || true
      - name: Run tests
        env:
          TEST_STRICT_SKIP: '1'
          SUITE: ${{ matrix.script }}
        run: |
          cd j2commerce/plg_j2commerce_productcompare/tests
          chmod +x run-tests.sh
          ./run-tests.sh "$SUITE"
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        if: always()
        with:
          name: j5-${{ matrix.script }}-logs
          path: j2commerce/plg_j2commerce_productcompare/tests/test-results/*.txt
      - name: Cleanup
        if: always()
        run: |
          cd j2commerce/plg_j2commerce_productcompare/tests
          docker compose down -v

  test-j6:
    name: ${{ matrix.name }} (J6)
    runs-on: ubuntu-latest
    needs: build
    permissions:
      contents: read
    strategy:
      fail-fast: false
      max-parallel: 1
      matrix:
        include:
          - name: '01 - Installation'
            script: 'installation'
          - name: '02 - Configuration'
            script: 'configuration'
          - name: '03 - Media Files'
            script: 'media'
          - name: '04 - Plugin Class'
            script: 'plugin-class'
          - name: '05 - AJAX Endpoint'
            script: 'ajax'
          - name: '06 - getProductsData'
            script: 'getproductsdata'
          - name: '07 - Asset Injection'
            script: 'asset-injection'
          - name: '08 - Render Injection'
            script: 'render-injection'
          - name: '09 - Event Dispatch'
            script: 'event-dispatch'
          - name: 'Installer Messages'
            script: 'install-messages'
          - name: '10 - Uninstall'
            script: 'uninstall'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: extension-package
          path: j2commerce/plg_j2commerce_productcompare/tests/
      - name: Setup PHP for J2Commerce build
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # v2
        with:
          php-version: '8.3'
          extensions: zip
      - name: Build J2Commerce 6 from source (pinned)
        run: |
          git init /tmp/j2commerce6-src
          git -C /tmp/j2commerce6-src remote add origin https://github.com/j2commerce/j2commerce.git
          git -C /tmp/j2commerce6-src fetch --depth 1 origin "$J2C6_REF"
          git -C /tmp/j2commerce6-src checkout --detach FETCH_HEAD
          echo "J2Commerce 6 source commit (pinned $J2C6_REF): $(git -C /tmp/j2commerce6-src rev-parse HEAD)"
          # The pinned J2Commerce 6 build minifies assets with esbuild (npm) unless --no-minify is passed;
          # the tests only need the PHP package, so the unminified build is used.
          cd /tmp/j2commerce6-src && php build/build_package.php --no-minify
          ZIP=$(ls docs/packages/pkg_j2commerce_*.zip docs/packages/com_j2commerce_*.zip 2>/dev/null | head -1)
          [ -z "$ZIP" ] && echo "ERROR: No J2Commerce 6 ZIP found" && ls docs/packages/ && exit 1
          sha256sum "$ZIP"
          cp "$ZIP" "$GITHUB_WORKSPACE/j2commerce/plg_j2commerce_productcompare/tests/j2commerce6.zip"
      - name: Start environment (J6)
        run: |
          cd j2commerce/plg_j2commerce_productcompare/tests
          docker compose -f docker-compose.joomla6.yml up -d
          echo "Waiting for container to be ready..."
          timeout 300 bash -c 'until docker exec plg_j2commerce_productcompare_j6_test test -f /var/www/html/health.txt 2>/dev/null; do sleep 5; done'
          echo "Container ready"
      - name: Show container logs
        if: always()
        run: |
          cd j2commerce/plg_j2commerce_productcompare/tests
          docker compose -f docker-compose.joomla6.yml logs joomla 2>&1 | tail -100 || true
      - name: Run tests
        env:
          TEST_STRICT_SKIP: '1'
          SUITE: ${{ matrix.script }}
        run: |
          cd j2commerce/plg_j2commerce_productcompare/tests
          chmod +x run-tests.sh
          J2COMMERCE_STACK=j6 CONTAINER_NAME=plg_j2commerce_productcompare_j6_test ./run-tests.sh "$SUITE"
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        if: always()
        with:
          name: j6-${{ matrix.script }}-logs
          path: j2commerce/plg_j2commerce_productcompare/tests/test-results/*.txt
      - name: Cleanup
        if: always()
        run: |
          cd j2commerce/plg_j2commerce_productcompare/tests
          docker compose -f docker-compose.joomla6.yml down -v

  official-j5-j2c4:
    name: Product Compare / official-j5-j2c4
    runs-on: ubuntu-latest
    needs: test-j5
    if: always()
    permissions: {}
    steps:
      - name: Verify official J5/J2C4 matrix
        env:
          RESULT: ${{ needs.test-j5.result }}
        run: |
          if [ "$RESULT" != "success" ]; then
            echo "Official J5/J2C4 matrix did not pass: $RESULT"
            exit 1
          fi

  official-j6-j2c6:
    name: Product Compare / official-j6-j2c6
    runs-on: ubuntu-latest
    needs: test-j6
    if: always()
    permissions: {}
    steps:
      - name: Verify official J6/J2C6 matrix
        env:
          RESULT: ${{ needs.test-j6.result }}
        run: |
          if [ "$RESULT" != "success" ]; then
            echo "Official J6/J2C6 matrix did not pass: $RESULT"
            exit 1
          fi

  # Result of this workflow. Deliberately not named "Collect Results": that
  # required check is produced only by collect-results.yml.
  all-jobs:
    name: Product Compare / all jobs
    runs-on: ubuntu-latest
    needs:
      - build
      - syntax-check
      - language-lint
      - test-j5
      - test-j6
      - official-j5-j2c4
      - official-j6-j2c6
    if: always()
    permissions:
      actions: read
    steps:
      - name: Download all test results
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          path: test-artifacts/
      - name: Collect and display logs
        run: |
          echo "=== Test Results Summary ==="
          for dir in test-artifacts/*/; do
            if [ -d "$dir" ]; then
              echo ""
              echo "--- $(basename "$dir") ---"
              find "$dir" -name "*.txt" -exec cat {} \;
            fi
          done
      - name: Upload combined logs
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: all-test-logs
          path: test-artifacts/
          retention-days: 30
      - name: Fail unless every job succeeded
        if: always()
        env:
          NEEDS_JSON: ${{ toJSON(needs) }}
        run: |
          echo "$NEEDS_JSON" | jq -r 'to_entries[] | "\(.key): \(.value.result)"'
          # failure, cancelled and skipped all count as not passed
          echo "$NEEDS_JSON" | jq -e 'all(.[]; .result == "success")' > /dev/null
